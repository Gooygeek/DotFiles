
# First clear the screen (no default message or login prompt)
clear
# Login welcome message
echo -e "\e[05;32m$(figlet -c KGOL.XYZ)\e[01m"

# Make sure all the other settings are still loaded properly
if [ -f ~/.bash_custom ]; then
    . ~/.bashrc
fi
