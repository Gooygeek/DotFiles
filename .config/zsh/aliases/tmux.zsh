# ensure tmux used 256 colours
alias tmux='tmux -2'
alias t='tmux -2'
# alias tm='tmux -2' # See the .bash.d folder for a better script
alias tml='tmux -2 list-sessions'
alias tmk='tmux -2 kill-session -t'
